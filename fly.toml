# Fly.io configuration
# Deploy with: fly launch && fly deploy

app = "confluence-slack-bot"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  # Non-secret env vars can go here
  # Secrets should be set with: fly secrets set KEY=value

[processes]
  app = "python main.py --reindex"

# No HTTP service needed - this is a worker bot
# Using Socket Mode for Slack connection

